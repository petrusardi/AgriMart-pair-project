<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title> Pendo Store </title>
    <link href="https:cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  </head>
  <style>
body {
  background: rgb(251, 251, 251);
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}    
.card {
  background: rgb(234, 237, 237);
  border-radius: 20px;
  display: inline-table;
  width: 200px;
  height: 400px;    
  text-align: center;
}
.img { 
  width: 200px;
  height: 150px;
}
.form {
  text-align: right;
}
.wspc {
  word-spacing: 10rem;
}
.title {
  text-align: center;
  word-wrap: normal;
  font-style: initial;
}
p {
  text-align: left;
}
  </style>
  <div class="container-fluid">
  <h1> My Employees</h1>
  <a href="/employees/add" class="btn btn-primary btn-lg">Add Employee</a>
  </div>

  <form action="/" method="get" class="form">
      <a>Sort By :</a>
   <a class="btn btn-outline-primary btn-sm" href="/?sort=name">Name</button></a>
      <a class="btn btn-outline-primary btn-sm" href="/?sort=position">Position</button></a>
      <select id="" class="collection-filter__sort-select-filter" name="education">
          <option selected disabled>Filter by Degree</option>
          <option class="option" value="Master Degree">Master Degree</option>
          <option class="option" value="Bachelor Degree">Bachelor Degree</option>
        </select>
        </a>
    <button type="submit" class="btn btn-primary btn-sm ">Filter</button>      
  </form>
</div>
  <div class="row row-cols-1 row-cols-md-3 g-4">
    <% for (let i = 0; i < employees.length; i++) { %>
    <div class="col-md-3 mb-3" style="width: 13rem;">
      <div class="card h-100">
        <img src="<%= employees[i].profile_picture %>" class="img" alt="">
        <div class="card-body">
          <h5 class="card-title"><%= employees[i].name %> (<%= employees[i].age %>)</h5>
          <h6><%= employees[i].position %> <br>
          (<%= employees[i].education %>)</h6>
          <br><br>
          <p>Contact: <br>
            <%= employees[i].email %> <%= employees[i].phone_number %> </p>
          <div class="button">
            <a href="/employees/edit/<%= employees[i].id %>" class="btn btn-outline-primary">Edit</a>
            <a href="/employees/delete/<%= employees[i].id %>" class="btn btn-danger">Delete</a>   
          </div>   
        </div>
      </div>
    </div>
    <% } %>
  </div>
</div>
</div>
