<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>PENDO STORE HOME</title>
    <link href="https:cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <%- include('_navbar') %>
  </head>
  <style>
    body {
      background: rgb(251, 251, 251);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }    
    .card {
      background: rgb(234, 237, 237);
      border-radius: 20px;
      display: inline-table;
      width: 200px;
      height: 400px;    
      text-align: center;
    }
    .img { 
      width: 200px;
      height: 150px;
    }
    .form {
      text-align: right;
    }
    .wspc {
      word-spacing: 10rem;
    }
    .title {
      text-align: center;
      word-wrap: normal;
      font-style: initial;
    }
    p {
      text-align: left;
    }
      </style>
  <body>
    <div class="row row-cols-1 row-cols-md-3 g-4 ms-4">
        <% data.forEach((el,idx) => { %>
          <div class="col-md-3 mb-3" style="width: 13rem;">
              <div class="card h-100">
                <img src="<%= el.imgUrl %>" class="img" alt="">
                <div class="card-body">
                  <h5 class="card-title"><%= el.name %></h5>
                  <h6><%= el.Category.name %> <br>
                  (<%= el.price %>)</h6>
                  <br><br>
                  <p>Description: <br>
                    <%= el.description %> </p>
                  <div class="button">
                    <a href="/products/<%= el.id %>" class="btn btn-outline-primary">Detail</a>
                    <a href="/cart" class="btn btn-success">Keranjang</a>   
                  </div>   
                </div>
              </div>
            </div>
          <% }) %>
      </div>
      </div>
  </body>
</html>